# Release Notes - v1.10.0 (2025-11-27)

## 🎯 모범 답안 및 에러 처리 개선

### ✨ 새로운 기능

#### 1. SCORM 모범 답안 기능
- **심화 학습 문제에 모범 답안 포함**
  - AI가 각 심화 문제 생성 시 200-300단어 분량의 모범 답안도 함께 생성
  - SCORM `advanced.html` 파일에서 "📖 모범 답안 확인하기" 드롭다운으로 표시
  - 학습자가 답변 작성 후 모범 답안과 비교하여 학습 효과 극대화
  - 오프라인 환경에서도 자가 학습 가능

**기술 구현**:
- `QuizItem` Pydantic 모델에 `model_answer?: str` 필드 추가
- `generate_advanced_learning` 프롬프트 수정으로 모범 답안 생성 요청
- HTML `<details>` 요소로 접이식 인터페이스 구현
- CSS 스타일링으로 깔끔한 드롭다운 디자인 적용

#### 2. 콘텐츠 재생성 버튼
- **생성 실패 시 즉시 복구 가능**
  - 개념 학습 또는 실습 과제 생성 실패 시 "🔄 콘텐츠 재생성" 버튼 표시
  - 버튼 클릭 시 캐시를 무시하고 새로운 생성 요청
  - 사용자가 에러 상황에서 손쉽게 복구 가능

**기술 구현**:
- `ChapterRequest`에 `force_refresh: bool` 파라미터 추가
- `ChapterPage.tsx`에 `handleRegenerate` 함수 구현
- 에러 감지 로직 (`content.concept.title === 'Error'`) 추가

### 🐛 버그 수정

#### 1. 에러 응답 캐싱 방지
- **문제**: 콘텐츠 생성 실패 시 에러 응답이 캐시에 저장되어 재시도해도 계속 에러 표시
- **해결**: `has_error` 플래그 도입, 에러 발생 시 캐시 및 DB 저장 생략

#### 2. AdvancedLearningResponse 검증 오류
- **문제**: 심화 학습 생성 실패 시 잘못된 에러 응답 구조로 500 에러 발생
- **해결**: 에러 응답도 올바른 Pydantic 모델 구조(`quizes` 배열) 반환

#### 3. SCORM 심화 학습 데이터 누락
- **문제**: SCORM 패키지 생성 시 `advanced_learning` 데이터를 누락하여 `advanced.html`이 비어있음
- **해결**: `download_chapter_scorm` 엔드포인트에서 `advanced_learning` 데이터 포함

#### 4. Markdown 다운로드 파일명 개선
- **문제**: 파일명에 챕터 번호가 없어 순서 파악이 어려움
- **해결**: `1_챕터제목.md` 형식으로 파일명 생성 (SCORM과 일관성 유지)

### 🔧 개선 사항

#### 에러 처리 강화
- `generate_chapter_content_only`에서 `asyncio.gather(return_exceptions=True)` 사용
- 개별 생성 태스크(개념/실습/퀴즈/심화) 실패 시에도 다른 부분은 정상 표시
- 부분 성공 허용으로 사용자 경험 개선

#### 로깅 개선
- 각 생성 단계별 상세 로그 출력
- 캐시 및 DB 저장 여부 로깅
- 에러 발생 시 구체적인 메시지 출력

#### 타입 안전성
- Frontend TypeScript: `AdvancedItem` 인터페이스에 `model_answer?: string` 추가
- Backend Pydantic: `QuizItem` 모델에 `model_answer: Optional[str]` 추가

### 📋 변경된 파일

#### Backend
- `app/main.py`: `force_refresh` 파라미터, 캐싱 로직, SCORM 데이터 수정
- `app/services/generator.py`: 모범 답안 생성 프롬프트 추가
- `app/services/scorm_service.py`: 모범 답안 렌더링, CSS 스타일 추가

#### Frontend
- `frontend/src/pages/ChapterPage.tsx`: 재생성 버튼, `handleRegenerate` 함수 추가
- `frontend/src/types/index.ts`: `AdvancedItem`, `ChapterRequest` 타입 업데이트

### 📚 문서
- `app/CHANGELOG.md`: v1.10.0 엔트리 추가
- `RELEASE_NOTES_v1.10.0.md`: 이 문서 (상세 릴리즈 노트)

### 🚀 사용 방법

#### 모범 답안 확인하기
1. 새로운 챕터 생성
2. SCORM 패키지 다운로드 (`📦 SCORM 1.2` 버튼)
3. `advanced.html` 열기
4. 각 심화 문제 아래 "📖 모범 답안 확인하기" 클릭

#### 콘텐츠 재생성하기
1. 챕터 페이지에서 개념/실습 탭 확인
2. "콘텐츠 생성에 실패했습니다" 메시지 확인
3. "🔄 콘텐츠 재생성" 버튼 클릭
4. 약 15초 후 새로운 콘텐츠 확인

### ⚠️ 주의사항
- 기존에 생성된 챕터는 모범 답안이 포함되지 않습니다
- 새로 생성하는 챕터부터 모범 답안이 포함됩니다
- `force_refresh`는  캐시를 무시하므로 매번 새로운 API 요청이 발생합니다

### 🔜 다음 버전 계획
- 퀴즈(객관식) 탭에도 재생성 버튼 추가
- 모범 답안 Markdown 형식 지원 (현재는 일반 텍스트)
- 개인화된 모범 답안 (사용자 수준에 맞춤)

---

**설치 및 업데이트**: Git pull 후 백엔드 서버 재시작 필요
